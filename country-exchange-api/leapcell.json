{
  "runtime": "python3.12",
  "build": {
    "command": "pip install -r requirements.txt"
  },
  "start": {
    "command": "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "env": {
    "PORT": "8000"
  }
}

